# Synthetic UMD Forge fixture

`umd-forge-test.iso` is a small synthetic ISO 9660 image used for tests and manual checks. It includes `PARAM.SFO`, `ICON0.PNG`, nested files and a deliberately fake `~PSP` EBOOT header.

The fixture contains no retail game data.
